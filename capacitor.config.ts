import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'complicated-cross-platform-universal-remote-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
